apiVersion: 1

providers:
  - name: vectorsearch-gateway
    orgId: 1
    folder: "vectorsearch-gateway"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
